#include <stdio.h>

int main() {
    int n;
    
    if (scanf("%d", &n) != 1) {
        return 0;
    }

    if (n < 0) {
        return 0;
    }

    for (int i = 1; i <= n; i++) {
        printf("[%d] Hello world", i);
        if (i < n) {
            printf("\n");
        }
    }

    return 0;
}
