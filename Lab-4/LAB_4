#include <stdio.h>

void GetSet(int set[], int n) {
    int i;

    printf("Enter the elements:\n");
    for (i = 0; i < n; i++) {
        scanf("%d", &set[i]);
    }
}

int main() {
    int set[100];
    int n;
    int i;

    printf("Enter number of elements: ");
    scanf("%d", &n);

    GetSet(set, n);

    printf("You entered: ");
    for (i = 0; i < n; i++) {
        printf("%d ", set[i]);
    }

    return 0;
}
